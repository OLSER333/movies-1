Сделать moviesApp

info : https://developers.themoviedb.org/3/authentication/how-do-i-generate-a-session-id

1. Create a new request token :
https://api.themoviedb.org/3/authentication/token/new?api_key=12c052732f00500a4355cf2bf4538874

2. Get the user to authorize the request token:
With a request token in hand, forward your user to the following URL:
https://www.themoviedb.org/authenticate/{REQUEST_TOKEN}

3.



Максим Морозов
Ребят, кто сделал задание с MovieDB, вопрос, как выглядит строка запроса изменения рейтинга фильма в гостевой сессии? Сначала отправляю запрос на создание гостевой сессии, получаю id, а дальше как не пытался изменить адрес запроса получаю Error 401 (ошибка аутентификации).
P.S. Комментарий про Local Storage прочитал, но хотелось бы разобраться с гостевой сессией
`https://api.themoviedb.org/3/movie/${id}/rating?api_key=${this.apiKey}&guest_session_id=${this.guestSessionId}`








Новые "проблемы":
1) открывает след страницу на том же месте, а лучше бы с начала
2) сортирует Rated как ему вздумается, а не самые новые вверх, как хотелось бы
    (-> переделка учёта localStorage)
3) infinity spin  если перезагрузить страницу без инета ( добавить tmdbApi .catch в App)


Известные проблемы :
Остаётся error пагиация после 1стр. +rate -> 36765 -> Rated -> Search -
отобр 20фильм 1 стр, но pag 36765

Дважды происходит updating (дважды fetching)
//==================================================================


Как должно работать:"
Searched - ставлю НОВЫЙ рейтинг (он сразу остаётся + ) --> turn: appear in Rated +
Searched - ОБНОВЛЯЮ рейтинг (он сразу остаётся + ) --> turn: appear in Rated (-)

Rated - УДАЛЯЮ из рейт (пока что остаётся + ) --> turn: deleted in Search&Rated (-)
Rated - ОБНОВЛЯЮ рейтинг (он сразу остаётся + ) --> turn: changed in Search (-)

Похоже у меня не обновляются rate, надо передать их вместе с пропсами при обновлении списка(!))

Проблема в том, что обновляется дважды хз что делать...

написать обработку 500+ страницы error..
разобраться с контекстом и избавиться от кучи аргументов?)

Ещё вариант - хранить в App копию localStorage - если
они не равны, то обновлять Rated, и соответственно в обратном порядке - обновлять

Правильно ли вставил key ?? в App

может быть полезно:
    const filmTitle = item.title || 'Movie title not specified';
    const overview = item.overview || 'Movie overview not specified';

Вопрос о localStorage -
Записывать данные в local. Есть 2 пути:
    1) toADD: сразу setItem(id, rate)
        а потом рендерить через {items} pushed через for(i = 0;;)
    2) toADD:  getItem(arrRatedMovies), JSON.parse, push new, JSON.stringify.
        но тогда getItem, parse, и делаем через родной map. Что быстрее?
App
    Tabs
        <TabsItem>
          //diff
            {needSearch && Input ...}
          //same
            {!movies && Spin}
            <ul.moviesList>
                movies.map(el => {
                    <Card>
                      onKeyDown(onChange)={() => onAdd(orChange)ToLocalStorage(id, Rate)
                    </Card>
                }

            </ul>
        <TabsItem/>

            {!movies && Spin}
            <ul.moviesList>
                ЦИКЛ FOR по LocStor.length
                    <Card>
                      onKeyDown(onChange)={() => onAdd(orChange)ToLocalStorage(id, Rate)
                    </Card>
                }
                pagination - разная логика. Там запрос, а тут надо брать прямо из local по 20 штук
            </ul>

        <TabsItem>


        <TabsItem/>






















